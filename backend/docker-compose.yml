services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./risklab.db:/app/risklab.db

  frontend:
    build: ../frontend
    ports:
      - "8501:8501"
    depends_on:
      - backend